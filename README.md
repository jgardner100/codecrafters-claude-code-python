
A simlpe script to pass AI queries across to OpenRouter and display the results.

1. Ensure you have `uv` installed locally.
2. Run `./your_program.sh` to run your program, which is implemented in
   `app/main.py`.

